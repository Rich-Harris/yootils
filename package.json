{
  "name": "yootils",
  "description": "Stuff I often need",
  "version": "0.0.9",
  "main": "yootils.umd.js",
  "module": "yootils.es.js",
  "types": "types/index.d.ts",
  "files": [
    "yootils.es.js",
    "yootils.umd.js",
    "src"
  ],
  "devDependencies": {
    "@types/mocha": "^5.2.5",
    "@types/node": "^10.7.1",
    "glob": "^7.1.2",
    "mocha": "^5.2.0",
    "rollup": "^0.64.1",
    "rollup-plugin-typescript": "^0.8.1",
    "rollup-plugin-virtual": "^1.0.1",
    "sander": "^0.6.0",
    "tiny-glob": "^0.2.2",
    "ts-node": "^7.0.1",
    "typescript": "^3.0.1"
  },
  "scripts": {
    "build-declarations": "tsc -d && node scripts/move-declaration-files.js",
    "build": "npm run build-declarations && rollup -c && node scripts/check-treeshaking.js",
    "test": "mocha --opts mocha.opts",
    "prepublishOnly": "npm run build && npm test"
  }
}
